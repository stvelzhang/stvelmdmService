server:
  port: 8866

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edthings?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:generator/*.xml
logging:
  level:
    root: warn


